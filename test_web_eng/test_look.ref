/*
 Сопоставляются ли выражения, одно из которых --- слово степени 2,
 а второе --- слово степени два с добавленным префиксом 1 и суффиксом 101?
 Ответ всегда нет.
 Тест на решение уравнений в словах (диофантовы уравнения на длины, квадратичные уравнения).
*/

/***********************************************/
/*          исходная программа                 */
/***********************************************/

$ENTRY Go {
	 (e.x) e.y = <F ('1' e.y e.y '101') (e.x e.x)>;
}

F {
	t.1 t.1 = 'T';
	e.z ='F';
}

/***********************************************/
/*          остаточная программа               */
/***********************************************/

/*

$ENTRY Go {
 (e.1) e.2 =  'F';
}
*/

/*
 Равенство в этом уравнении скрыто в первой строке определения функции F.
 Образцы допускают наличие двух одинаковых t-параметров в них, но если эти
 параметры сопоставляются с выражениями в скобках, возникает вопрос о разрешении
 равенства двух произвольных выражений.

 В случае выше было порождено уравнение
 '1' (weval e x)(weval e x) '101' = (weval e y)(weval e y).

 Это квадратичное уравнение в словах, не имеющее корней. Поэтому ветвь, его
 содержащая, была отброшена.
*/
